import s from "./FeedbackItem.module.scss";

export const FeedbackItem = ({ id, name, desc, text, textExtended }) => {
    console.log(id, name, desc, text, textExtended);
    
    return (
        <div className={s.wrap}>
            saa
        </div>
    );
};
